Field (header-body) based PDF creator written in java; with JSON user configuration  

Dependencies: PDFBox, Lombok, Jackson, Bouncycastle (check pom.xml)

optionally digitally signs using an aliased keystore under javax.ssl.net.keyStore (password under .keyStorePassword)
